# 作業練習，以及筆記
## 關鍵概念解析：
- setInterval()：每隔指定的毫秒數重複執行一段程式碼，用來模擬時鐘的「每秒跳動」。
- new Date().getTime()：取得自 1970 年 1 月 1 日至當前的總毫秒數，方便我們進行數學加減計算。
- Math.floor()：無條件捨去，將毫秒換算為整數的分鐘與秒數。
- padStart(2, '0')：如果數字只有 1 位數（例如 5 秒），自動在前方補 0 變成 05，讓視覺更符合時鐘格式。
- clearInterval()：當倒數結束時，用來停止 setInterval() 避免資源持續消耗。